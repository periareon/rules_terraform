"""Sub-module referenced by the root module via `source = "./modules/greeter"`."""

load("@rules_terraform//terraform:terraform_module.bzl", "terraform_module")

terraform_module(
    name = "greeter",
    srcs = glob(["*.tf"]),
    visibility = ["//visibility:public"],
)
